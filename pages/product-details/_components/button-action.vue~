<script setup lang="ts">
import AmountButton from "./amount-button.vue";
import { Button } from "@/components/ui/button";
import { Separator } from "@/components/ui/separator";

// Define a method to show an alert
const showAlert = (message: string) => {
  alert(message);
};
</script>

<template>
  <div class="flex flex-col gap-4 items-center w-full md:flex-row">
    <AmountButton />
    <Button class="flex gap-4 w-full" @click="showAlert('Added to cart!')">
      <Icon name="tabler:shopping-bag-plus" />
      Add to cart
    </Button>
    <Separator class="w-4" label="Or" />
    <Button class="flex gap-4 w-full" @click="showAlert('Added to cart!')">
      <Icon name="tabler:shopping-bag-plus" />
      Make your order
    </Button>
  </div>
</template>
